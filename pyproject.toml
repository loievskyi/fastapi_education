[project]
name = "fastapi-education"
version = "0.1.0"
description = "Educational project built with FastAPI."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
]
